<block>
    <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}">
        <view wx:key="$original.value" bindtap="handleClick" class="{{option.$original.disabled ? 'at-checkbox__option at-checkbox__option--disabled' : 'at-checkbox__option'}}" wx:for="{{loopArray0}}" wx:for-item="option" data-e-tap-so="this" data-e-tap-a-a="{{option.$original}}">
            <view class="at-checkbox__option_wrap">
                <block wx:if="{{option.$original.desc}}">
                    <view class="at-checkbox__desc">{{option.$original.desc}}</view>
                </block>
                <view class="at-checkbox__option_container">
                    <view class="{{option.$loopState__temp4}}">
                        <view class="at-checkbox__icon_container">
                            <at-icon customStyle="{{option.$loopState__temp6}}" __triggerObserer="{{ _triggerObserer }}" value="check" color="#fff"></at-icon>
                        </view>
                    </view>
                    <view class="at-checkbox__title">{{option.$original.label}}</view>
                </view>
            </view>
        </view>
    </view>
</block>